import { Meteor } from 'meteor/meteor';

import Bar from '../imports/api/bar';
import Foo from '../imports/api/foo';
import '../imports/startup/data';

Meteor.startup(() => {
  // code to run on server at startup
});
